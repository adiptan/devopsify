{
  "nginx": [
    {
      "id": "nginx_logs_location",
      "title": "Nginx логи (расположение)",
      "content": "**Где находятся логи Nginx?**\n\n**Стандартное расположение:**\n```bash\n/var/log/nginx/access.log   # Все запросы\n/var/log/nginx/error.log    # Ошибки\n```\n\n**Как узнать точное расположение:**\n```bash\n# Смотрим в конфиге\ngrep -r \"access_log\" /etc/nginx/\ngrep -r \"error_log\" /etc/nginx/\n\n# Или через nginx -T (dump config)\nnginx -T | grep log\n```\n\n**Где задаётся:**\n```nginx\n# /etc/nginx/nginx.conf\nhttp {\n    access_log /var/log/nginx/access.log;\n    error_log /var/log/nginx/error.log;\n}\n\n# Можно переопределить в server блоке\nserver {\n    access_log /var/log/nginx/mysite.access.log;\n    error_log /var/log/nginx/mysite.error.log;\n}\n```\n\n**Просмотр логов:**\n```bash\ntail -f /var/log/nginx/access.log  # Follow режим\njournalctl -u nginx -f              # Через systemd\n```",
      "example": "tail -f /var/log/nginx/access.log"
    },
    {
      "id": "nginx_access_log_format",
      "title": "Структура access.log (default)",
      "content": "**Формат логов Nginx (default — combined)**\n\n**Пример строки:**\n```\n127.0.0.1 - - [30/Mar/2026:09:15:22 +0300] \"GET /index.html HTTP/1.1\" 200 612 \"-\" \"Mozilla/5.0\"\n```\n\n**Разбор по полям:**\n1. **127.0.0.1** — IP адрес клиента ($remote_addr)\n2. **-** — Remote log name (обычно `-`)\n3. **-** — Remote user (HTTP auth, обычно `-`)\n4. **[30/Mar/2026:09:15:22 +0300]** — Timestamp ($time_local)\n5. **\"GET /index.html HTTP/1.1\"** — HTTP запрос ($request)\n   - Метод: GET\n   - URI: /index.html\n   - Версия: HTTP/1.1\n6. **200** — Status code ($status)\n7. **612** — Размер ответа в байтах ($body_bytes_sent)\n8. **\"-\"** — Referer (откуда пришли, $http_referer)\n9. **\"Mozilla/5.0\"** — User-Agent ($http_user_agent)\n\n**Как использовать в awk:**\n```bash\n# Поле 1 — IP адрес\nawk '{print $1}' access.log\n\n# Поле 6 — HTTP метод (убираем кавычку)\nawk '{print $6}' access.log | tr -d '\"'\n\n# Поле 9 — Status code\nawk '{print $9}' access.log\n```",
      "example": "awk '{print $1, $9}' /var/log/nginx/access.log | head"
    }
  ],
  "bash": [
    {
      "id": "bash_sort_keys",
      "title": "sort (ключи)",
      "content": "**Важные ключи sort:**\n\n**Числовая сортировка (-n):**\n```bash\n# Неправильно (строковая):\necho -e \"10\\n2\\n100\" | sort\n# 10, 100, 2\n\n# Правильно (числовая):\necho -e \"10\\n2\\n100\" | sort -n\n# 2, 10, 100\n```\n\n**Другие ключи:**\n```bash\nsort -r file.txt          # Обратный порядок\nsort -rn file.txt         # Числовой обратный\nsort -k2 file.txt         # По 2-й колонке\nsort -t':' -k3 -n /etc/passwd  # Разделитель ':'\n```\n\n**Частый паттерн:**\n```bash\ncat access.log | awk '{print $1}' | sort | uniq -c | sort -rn\n```",
      "example": "sort -n numbers.txt"
    },
    {
      "id": "bash_uniq",
      "title": "uniq (особенности)",
      "content": "**⚠️ ВАЖНО: uniq работает ТОЛЬКО с соседними строками!**\n\n**Неправильно:**\n```bash\ncat file.txt | uniq -c  # Не схлопнет дубликаты!\n```\n\n**Правильно:**\n```bash\ncat file.txt | sort | uniq -c  # Сначала sort!\n```\n\n**Ключи uniq:**\n```bash\nuniq file.txt          # Уникальные строки\nuniq -c file.txt       # Подсчитать повторения\nuniq -d file.txt       # Только дубликаты\n```\n\n**Типичная ошибка на собесе:**\n```bash\n# Топ-5 IP (неправильно)\nawk '{print $1}' access.log | uniq -c | sort -rn\n\n# Правильно:\nawk '{print $1}' access.log | sort | uniq -c | sort -rn\n```",
      "example": "cat file.txt | sort | uniq -c"
    },
    {
      "id": "bash_pstree",
      "title": "pstree (дерево процессов)",
      "content": "**Просмотр дерева процессов:**\n\n```bash\n# Простое дерево\npstree\n\n# С PID\npstree -p\n\n# С аргументами команд\npstree -a\n\n# От конкретного процесса\npstree -p 1234\n\n# Конкретный пользователь\npstree username\n```\n\n**Пример вывода:**\n```\nsystemd─┬─sshd───sshd───bash───pstree\n        ├─nginx───nginx\n        └─docker─┬─containerd\n                 └─...\n```\n\n**Альтернативы:**\n```bash\nps -ejH          # Tree view\nps axjf          # Forest view\n```",
      "example": "pstree -p"
    },
    {
      "id": "bash_lsof_advanced",
      "title": "lsof (все применения)",
      "content": "**lsof = list open files (НЕ только \"кто использует файл\"!)**\n\n**Все открытые файлы процесса:**\n```bash\nlsof -p 1234\n```\n\n**Сетевые соединения:**\n```bash\nlsof -i              # Все сокеты\nlsof -i :80          # Кто слушает порт 80\nlsof -i TCP          # Только TCP\nlsof -i @8.8.8.8     # Соединения с 8.8.8.8\n```\n\n**Кто использует файл:**\n```bash\nlsof /var/log/nginx/access.log\n```\n\n**Файлы пользователя:**\n```bash\nlsof -u nginx\n```\n\n**Комбинации:**\n```bash\nlsof -i -a -p 1234   # Сетевые соединения процесса\n```",
      "example": "lsof -i :80"
    }
  ],
  "kubernetes": {
    "title": "Kubernetes",
    "cards": [
      {
        "id": 1,
        "title": "Что такое Kubernetes",
        "content": "Kubernetes (K8s) — это платформа оркестрации контейнеров, автоматизирующая развёртывание, масштабирование и управление контейнеризованными приложениями. Разработан Google, сейчас поддерживается Cloud Native Computing Foundation (CNCF).\n\nОсновные задачи K8s: автоматическое размещение контейнеров на узлах (scheduling), self-healing (перезапуск упавших контейнеров), горизонтальное масштабирование, service discovery и балансировка нагрузки, управление конфигурациями и секретами.\n\nПочему Kubernetes: стандарт индустрии для оркестрации, поддержка мультиоблачности (AWS, GCP, Azure), огромная экосистема инструментов (Helm, Istio, Prometheus), декларативное управление инфраструктурой.",
        "example": "# Проверить версию kubectl и подключение к кластеру\nkubectl version --short\nkubectl cluster-info\n\n# Посмотреть узлы кластера\nkubectl get nodes\n\n# Посмотреть все ресурсы во всех namespace\nkubectl get all --all-namespaces",
        "key_points": [
          "Оркестрация контейнеров — автоматизация развёртывания и масштабирования",
          "Self-healing — автоматический перезапуск упавших подов",
          "Декларативный подход — описываешь желаемое состояние, K8s его достигает"
        ]
      },
      {
        "id": 2,
        "title": "Архитектура: Control Plane и Nodes",
        "content": "Kubernetes кластер состоит из Control Plane (управляющий слой) и Worker Nodes (рабочие узлы). Control Plane управляет кластером, Worker Nodes запускают контейнеры.\n\nControl Plane компоненты: API Server (точка входа для всех операций), etcd (распределённое key-value хранилище состояния), Scheduler (размещает поды на узлах), Controller Manager (отслеживает состояние ресурсов и приводит к желаемому).\n\nWorker Node компоненты: kubelet (агент на узле, управляет подами), kube-proxy (сетевая маршрутизация), Container Runtime (Docker, containerd, CRI-O — запускает контейнеры).",
        "example": "# Посмотреть компоненты Control Plane\nkubectl get pods -n kube-system\n\n# Детальная информация об узле\nkubectl describe node <node-name>\n\n# Посмотреть события в кластере\nkubectl get events --all-namespaces\n\n# Статус компонентов\nkubectl get componentstatuses",
        "key_points": [
          "Control Plane — мозг кластера (API Server, Scheduler, etcd)",
          "Worker Nodes — исполнители (kubelet + container runtime)",
          "etcd — единственное место хранения состояния кластера (критично для бэкапов)"
        ]
      },
      {
        "id": 3,
        "title": "Pods: базовая единица",
        "content": "Pod — минимальная единица развёртывания в Kubernetes. Pod содержит один или несколько контейнеров, которые разделяют сеть и storage. Обычно в поде один основной контейнер + опциональные sidecar-контейнеры (логирование, мониторинг).\n\nПоды эфемерны — они могут быть удалены и пересозданы в любой момент. IP-адрес пода меняется при пересоздании — поэтому для доступа используются Services.\n\nПод описывается в YAML-манифесте: apiVersion, kind: Pod, metadata (имя, labels), spec (контейнеры, volumes, политики перезапуска). Контейнеры в поде общаются через localhost.",
        "example": "# Создать под из манифеста\nkubectl apply -f pod.yaml\n\n# Посмотреть поды\nkubectl get pods\nkubectl get pods -o wide  # с IP и узлами\n\n# Детальная информация о поде\nkubectl describe pod my-pod\n\n# Логи контейнера в поде\nkubectl logs my-pod\nkubectl logs my-pod -c container-name  # если несколько контейнеров\n\n# Выполнить команду в поде\nkubectl exec -it my-pod -- /bin/bash",
        "key_points": [
          "Pod = 1+ контейнеров с общей сетью и storage",
          "Поды эфемерны — не хранят состояние, могут быть пересозданы",
          "Обычно не создаём поды напрямую — используем Deployments"
        ]
      },
      {
        "id": 4,
        "title": "Deployments: управление подами",
        "content": "Deployment — контроллер для управления подами. Deployment гарантирует, что нужное количество реплик подов всегда запущено. Если под упал — Deployment создаст новый.\n\nDeployment использует ReplicaSet (контроллер для поддержания количества реплик) под капотом. Deployment позволяет делать rolling updates (постепенное обновление без даунтайма) и rollback (откат к предыдущей версии).\n\nОсновные поля: replicas (количество подов), selector (как найти свои поды по labels), template (шаблон пода), strategy (стратегия обновления — RollingUpdate или Recreate).",
        "example": "# Создать Deployment\nkubectl create deployment nginx --image=nginx:1.21 --replicas=3\n\n# Посмотреть Deployments\nkubectl get deployments\n\n# Посмотреть ReplicaSets (создаются автоматически)\nkubectl get replicasets\n\n# Обновить образ (rolling update)\nkubectl set image deployment/nginx nginx=nginx:1.22\n\n# Посмотреть статус обновления\nkubectl rollout status deployment/nginx\n\n# Откатить к предыдущей версии\nkubectl rollout undo deployment/nginx\n\n# Масштабирование\nkubectl scale deployment/nginx --replicas=5",
        "key_points": [
          "Deployment — декларативное управление подами",
          "Rolling update — обновление без даунтайма",
          "Автоматический self-healing — пересоздание упавших подов"
        ]
      },
      {
        "id": 5,
        "title": "Services: сетевое взаимодействие",
        "content": "Service — абстракция для доступа к подам. Поды эфемерны и меняют IP, Service предоставляет стабильный IP и DNS-имя. Service автоматически балансирует трафик между подами.\n\nТипы Services: ClusterIP (доступ только внутри кластера, по умолчанию), NodePort (открывает порт на всех узлах для внешнего доступа), LoadBalancer (создаёт внешний балансировщик в облаке), ExternalName (DNS alias).\n\nService находит свои поды через selector (соответствие labels). Трафик распределяется между всеми подами с этими labels.",
        "example": "# Создать Service для Deployment\nkubectl expose deployment nginx --port=80 --type=ClusterIP\n\n# Посмотреть Services\nkubectl get services\n\n# DNS-имя Service: <service-name>.<namespace>.svc.cluster.local\n# Из пода можно обратиться: curl http://nginx\n\n# NodePort (доступ снаружи через <NodeIP>:<NodePort>)\nkubectl expose deployment nginx --type=NodePort --port=80\n\n# LoadBalancer (для облачных кластеров)\nkubectl expose deployment nginx --type=LoadBalancer --port=80\n\n# Детальная информация\nkubectl describe service nginx",
        "key_points": [
          "Service — стабильный IP и DNS для группы подов",
          "selector — как Service находит свои поды (по labels)",
          "ClusterIP — внутри кластера, NodePort — снаружи, LoadBalancer — облако"
        ]
      },
      {
        "id": 6,
        "title": "ConfigMaps и Secrets",
        "content": "ConfigMap — хранилище конфигурационных данных (не секретных). Используется для передачи настроек в контейнеры: переменные окружения, конфигурационные файлы, параметры командной строки.\n\nSecret — хранилище секретных данных (пароли, токены, ключи). Данные хранятся в base64 (не шифрование, просто кодировка!). Для реального шифрования используй внешние решения (HashiCorp Vault, Sealed Secrets).\n\nИспользование: монтирование как volume (файлы в контейнере) или как переменные окружения (env, envFrom). ConfigMap/Secret изменяются без пересборки образа — изменил конфиг, перезапустил под.",
        "example": "# Создать ConfigMap\nkubectl create configmap app-config --from-literal=DATABASE_URL=postgres://db:5432\n\n# Создать ConfigMap из файла\nkubectl create configmap app-config --from-file=config.yaml\n\n# Создать Secret\nkubectl create secret generic db-password --from-literal=password=mypassword\n\n# Посмотреть ConfigMaps/Secrets\nkubectl get configmaps\nkubectl get secrets\n\n# Использование в поде (переменная окружения)\n# spec:\n#   containers:\n#   - env:\n#     - name: DATABASE_URL\n#       valueFrom:\n#         configMapKeyRef:\n#           name: app-config\n#           key: DATABASE_URL",
        "key_points": [
          "ConfigMap — конфигурация, Secret — секреты",
          "Secrets в base64 — не шифрование! Используй Vault для реальной защиты",
          "Изменение ConfigMap/Secret не пересоздаёт под автоматически"
        ]
      },
      {
        "id": 7,
        "title": "Volumes: персистентное хранилище",
        "content": "Volumes в Kubernetes позволяют подам хранить данные, которые переживают перезапуск контейнера. Типы volumes: emptyDir (временное хранилище, удаляется с подом), hostPath (монтирует директорию с узла), PersistentVolume (внешнее хранилище).\n\nPersistentVolume (PV) — ресурс хранилища в кластере (например, AWS EBS, NFS). PersistentVolumeClaim (PVC) — запрос на выделение хранилища. Под монтирует PVC, K8s автоматически привязывает PV.\n\nStorageClass — шаблон для динамического создания PV. Например, StorageClass для AWS EBS автоматически создаст диск при создании PVC.",
        "example": "# Создать PersistentVolumeClaim\napiVersion: v1\nkind: PersistentVolumeClaim\nmetadata:\n  name: my-pvc\nspec:\n  accessModes:\n    - ReadWriteOnce\n  resources:\n    requests:\n      storage: 10Gi\n\n# Использовать PVC в поде\n# spec:\n#   volumes:\n#   - name: data\n#     persistentVolumeClaim:\n#       claimName: my-pvc\n#   containers:\n#   - volumeMounts:\n#     - name: data\n#       mountPath: /data\n\n# Посмотреть PV и PVC\nkubectl get pv\nkubectl get pvc",
        "key_points": [
          "emptyDir — временное, hostPath — узел, PV — внешнее хранилище",
          "PVC — запрос на хранилище, PV — выделенный ресурс",
          "StorageClass — динамическое создание PV при создании PVC"
        ]
      },
      {
        "id": 8,
        "title": "Namespaces: изоляция ресурсов",
        "content": "Namespace — виртуальный кластер внутри физического кластера. Позволяет разделять ресурсы между командами, проектами или окружениями (dev, staging, prod).\n\nПо умолчанию есть namespaces: default (если не указан другой), kube-system (системные компоненты K8s), kube-public (публично доступные ресурсы), kube-node-lease (heartbeats узлов).\n\nИзоляция: ресурсы в разных namespaces не видят друг друга напрямую (нужен полный DNS-путь). ResourceQuota ограничивает ресурсы namespace (CPU, память, количество подов). NetworkPolicy изолирует сетевой трафик.",
        "example": "# Создать namespace\nkubectl create namespace dev\n\n# Посмотреть namespaces\nkubectl get namespaces\n\n# Создать ресурс в namespace\nkubectl apply -f deployment.yaml -n dev\n\n# Посмотреть ресурсы в namespace\nkubectl get pods -n dev\n\n# Переключить дефолтный namespace\nkubectl config set-context --current --namespace=dev\n\n# DNS между namespaces:\n# <service>.<namespace>.svc.cluster.local\n# Пример: curl http://nginx.dev.svc.cluster.local",
        "key_points": [
          "Namespace — логическое разделение ресурсов",
          "default — дефолтный namespace если не указан другой",
          "ResourceQuota — лимиты ресурсов для namespace"
        ]
      },
      {
        "id": 9,
        "title": "kubectl: основные команды",
        "content": "kubectl — CLI для управления Kubernetes. Основные команды: get (просмотр ресурсов), describe (детальная информация), apply (создать/обновить из манифеста), delete (удалить), logs (логи контейнера), exec (выполнить команду в контейнере).\n\nФлаги: -n <namespace> (указать namespace), -o yaml/json (вывод в формате), --all-namespaces (все namespaces), -w (watch — отслеживать изменения), -f <file> (файл манифеста).\n\nОтладка: kubectl describe — детальная инфа + события, kubectl logs — логи контейнера, kubectl exec -it — интерактивная оболочка в контейнере, kubectl get events — все события кластера.",
        "example": "# Посмотреть ресурсы\nkubectl get pods\nkubectl get deployments -o wide\nkubectl get all -n kube-system\n\n# Детальная информация\nkubectl describe pod my-pod\n\n# Создать/обновить из манифеста\nkubectl apply -f deployment.yaml\n\n# Удалить ресурс\nkubectl delete pod my-pod\nkubectl delete -f deployment.yaml\n\n# Логи\nkubectl logs my-pod -f  # follow\n\n# Выполнить команду\nkubectl exec -it my-pod -- bash\n\n# Проброс порта (локально → под)\nkubectl port-forward pod/my-pod 8080:80",
        "key_points": [
          "kubectl apply -f — создать/обновить (декларативный подход)",
          "kubectl get -o yaml — посмотреть полный манифест ресурса",
          "kubectl logs -f — мониторинг логов в реальном времени"
        ]
      },
      {
        "id": 10,
        "title": "Best practices",
        "content": "Best practices Kubernetes: используй Deployments вместо голых Pods, настраивай resource limits (CPU/memory) для предотвращения перегрузки узлов, используй liveness/readiness probes для health checks.\n\nБезопасность: не используй latest-теги образов (указывай конкретные версии), храни секреты в Secret (не в ConfigMap), используй RBAC для контроля доступа, запускай контейнеры от non-root пользователя.\n\nОрганизация: используй namespaces для разделения окружений, добавляй labels для группировки и фильтрации, используй annotations для метаданных, храни манифесты в Git (GitOps подход).",
        "example": "# Resource limits в Deployment\nresources:\n  requests:\n    memory: \"64Mi\"\n    cpu: \"250m\"\n  limits:\n    memory: \"128Mi\"\n    cpu: \"500m\"\n\n# Liveness probe (перезапуск если не отвечает)\nlivenessProbe:\n  httpGet:\n    path: /healthz\n    port: 8080\n  initialDelaySeconds: 30\n  periodSeconds: 10\n\n# Readiness probe (не слать трафик пока не готов)\nreadinessProbe:\n  httpGet:\n    path: /ready\n    port: 8080\n  initialDelaySeconds: 5\n  periodSeconds: 5",
        "key_points": [
          "Resource limits — предотвращают перегрузку узлов",
          "Probes — liveness для перезапуска, readiness для трафика",
          "GitOps — манифесты в Git, автоматический deploy через CI/CD"
        ]
      }
    ],
    "tasks": [
      21,
      22,
      23
    ]
  },
  "git": {
    "title": "Git",
    "cards": [
      {
        "id": 1,
        "title": "Что такое Git",
        "content": "Git — распределённая система контроля версий, созданная Линусом Торвальдсом для разработки ядра Linux. Git отслеживает изменения в коде, позволяет работать параллельно в ветках, объединять изменения и откатывать ошибки.\n\nПреимущества Git: распределённость (у каждого полная копия репозитория), скорость (локальные операции мгновенны), ветвление (дёшево и быстро), целостность данных (каждый коммит имеет SHA-1 хеш).\n\nОсновные понятия: репозиторий (хранилище кода), коммит (снимок состояния кода), ветка (независимая линия разработки), remote (удалённый репозиторий на GitHub/GitLab).",
        "example": "# Установка Git\nsudo apt install git  # Ubuntu/Debian\nsudo yum install git  # CentOS/RHEL\n\n# Проверить версию\ngit --version\n\n# Настроить пользователя (обязательно!)\ngit config --global user.name \"Your Name\"\ngit config --global user.email \"your@email.com\"\n\n# Посмотреть настройки\ngit config --list",
        "key_points": [
          "Распределённая система — каждый имеет полную копию истории",
          "Коммит — снимок состояния кода в момент времени",
          "SHA-1 хеш — уникальный идентификатор коммита"
        ]
      },
      {
        "id": 2,
        "title": "git init и git clone",
        "content": "git init создаёт новый репозиторий в текущей директории. Создаётся скрытая папка .git, в которой хранятся все данные Git: история коммитов, ветки, конфигурация.\n\ngit clone копирует существующий удалённый репозиторий на локальную машину. Автоматически настраивается remote origin (ссылка на исходный репозиторий).\n\nПосле git init нужно добавить remote вручную (git remote add origin <url>), если планируешь пушить на сервер (GitHub, GitLab).",
        "example": "# Создать новый репозиторий\nmkdir myproject && cd myproject\ngit init\n# Появится .git/ директория\n\n# Клонировать существующий репозиторий\ngit clone https://github.com/user/repo.git\ncd repo\n\n# Клонировать в конкретную директорию\ngit clone https://github.com/user/repo.git my-folder\n\n# Посмотреть remotes\ngit remote -v",
        "key_points": [
          "git init — новый репозиторий, git clone — копия существующего",
          ".git/ — скрытая папка с историей и настройками",
          "origin — стандартное имя для основного remote"
        ]
      },
      {
        "id": 3,
        "title": "git add и git commit",
        "content": "git add добавляет файлы в staging area (индекс) — промежуточное состояние перед коммитом. Можно добавить конкретные файлы (git add file.txt) или все изменения (git add .).\n\ngit commit сохраняет изменения из staging area в локальный репозиторий. Каждый коммит имеет сообщение (commit message), автора, дату и уникальный SHA-1 хеш.\n\nХорошие commit messages: краткое описание (до 50 символов), используй глаголы в повелительном наклонении (Add feature, Fix bug), детальное описание в теле коммита если нужно.",
        "example": "# Посмотреть статус (какие файлы изменены)\ngit status\n\n# Добавить файл в staging\ngit add README.md\n\n# Добавить все изменения\ngit add .\n\n# Коммит с сообщением\ngit commit -m \"Add README with project description\"\n\n# Коммит с добавлением всех изменённых файлов (пропустить git add)\ngit commit -am \"Fix typo in documentation\"\n\n# Изменить последний коммит (добавить файлы или изменить message)\ngit commit --amend",
        "key_points": [
          "Staging area — промежуточный этап перед коммитом",
          "git add . — добавить все изменения",
          "Хорошее commit message — краткое, понятное, описывает ЧТО и ЗАЧЕМ"
        ]
      },
      {
        "id": 4,
        "title": "git status, git log, git diff",
        "content": "git status показывает текущее состояние: какие файлы изменены, что в staging, что не отслеживается. Это первая команда при работе с репозиторием.\n\ngit log отображает историю коммитов: автор, дата, сообщение, хеш. Флаги: --oneline (краткий формат), --graph (визуализация веток), --all (все ветки), -n 5 (последние 5 коммитов).\n\ngit diff показывает изменения: git diff (unstaged изменения), git diff --staged (изменения в staging), git diff HEAD (все изменения с последнего коммита), git diff <commit1> <commit2> (между коммитами).",
        "example": "# Статус репозитория\ngit status\n\n# История коммитов\ngit log\ngit log --oneline --graph --all  # красивый вывод\ngit log --author=\"John\" --since=\"2 weeks ago\"\n\n# Изменения в файлах\ngit diff  # unstaged изменения\ngit diff --staged  # staged изменения\ngit diff HEAD~1  # изменения относительно предыдущего коммита\n\n# Изменения конкретного файла\ngit diff README.md",
        "key_points": [
          "git status — первая команда для проверки состояния",
          "git log --oneline --graph — удобный формат истории",
          "git diff — видеть изменения перед коммитом"
        ]
      },
      {
        "id": 5,
        "title": "Ветки: git branch, git checkout",
        "content": "Ветки (branches) — независимые линии разработки. Создавай ветку для каждой фичи или багфикса, затем вливай в main через merge.\n\ngit branch показывает список веток, git branch <name> создаёт новую ветку, git checkout <name> переключается на ветку. С Git 2.23+ есть git switch — более понятная команда для переключения.\n\nВетка main (или master) — основная ветка. Feature branches — ветки для разработки фич. Release branches — ветки для релизов. Hotfix branches — срочные исправления на продакшене.",
        "example": "# Посмотреть ветки\ngit branch\n\n# Создать новую ветку\ngit branch feature/add-login\n\n# Переключиться на ветку\ngit checkout feature/add-login\n\n# Создать и переключиться одной командой\ngit checkout -b feature/add-login\n\n# Новый способ (Git 2.23+)\ngit switch feature/add-login\ngit switch -c feature/new-feature  # создать и переключиться\n\n# Удалить ветку\ngit branch -d feature/add-login\ngit branch -D feature/add-login  # принудительно",
        "key_points": [
          "Одна ветка = одна фича/багфикс",
          "git checkout -b — создать и переключиться одной командой",
          "main/master — основная ветка, feature/* — ветки фич"
        ]
      },
      {
        "id": 6,
        "title": "git merge: объединение веток",
        "content": "git merge объединяет изменения из одной ветки в другую. Типичный workflow: разработка в feature-ветке, затем merge в main.\n\nТипы merge: fast-forward (если main не изменялся — просто перемещается указатель), three-way merge (если обе ветки имеют новые коммиты — создаётся merge commit).\n\nКонфликты возникают когда одни и те же строки изменены в обеих ветках. Git помечает конфликты в файлах: <<<<<<< HEAD ... ======= ... >>>>>>>. Разреши конфликт вручную, затем git add и git commit.",
        "example": "# Переключиться на целевую ветку\ngit checkout main\n\n# Влить изменения из feature-ветки\ngit merge feature/add-login\n\n# Если конфликт:\n# 1. Открой файл, разреши конфликт (удали маркеры <<<<, ====, >>>>)\n# 2. git add <file>\n# 3. git commit\n\n# Отменить merge (если не закоммичен)\ngit merge --abort\n\n# Посмотреть ветки, которые уже влиты в main\ngit branch --merged\n\n# Удалить влитые ветки\ngit branch -d feature/add-login",
        "key_points": [
          "Fast-forward — main просто двигается вперёд (нет merge commit)",
          "Three-way merge — создаётся merge commit с двумя родителями",
          "Конфликты — разрешай вручную, затем git add + commit"
        ]
      },
      {
        "id": 7,
        "title": "git rebase: линейная история",
        "content": "git rebase переносит коммиты из одной ветки на другую, создавая линейную историю (без merge commits). Rebase перезаписывает историю — коммиты получают новые SHA-1 хеши.\n\nКогда использовать: для очистки истории перед merge в main, для синхронизации feature-ветки с актуальным main. Когда НЕ использовать: на публичных ветках (main, shared branches) — перезапись истории сломает работу других.\n\nИнтерактивный rebase (git rebase -i) позволяет редактировать историю: изменять сообщения коммитов, объединять коммиты (squash), удалять, переставлять порядок.",
        "example": "# Обновить feature-ветку изменениями из main\ngit checkout feature/add-login\ngit rebase main\n\n# Если конфликт — разрешить, затем:\ngit add <file>\ngit rebase --continue\n\n# Отменить rebase\ngit rebase --abort\n\n# Интерактивный rebase (последние 3 коммита)\ngit rebase -i HEAD~3\n# Откроется редактор:\n# pick abc1234 First commit\n# squash def5678 Second commit  # объединить с предыдущим\n# reword ghi9012 Third commit   # изменить сообщение",
        "key_points": [
          "Rebase — линейная история, merge — ветвистая",
          "НЕ используй rebase на публичных ветках (перезаписывает историю)",
          "git rebase -i — редактирование истории (squash, reword)"
        ]
      },
      {
        "id": 8,
        "title": "Merge vs Rebase: что выбрать",
        "content": "Merge и rebase решают одну задачу (интеграция изменений) разными способами. Merge сохраняет историю как есть, создавая merge commits. Rebase переписывает историю, делая её линейной.\n\nПреимущества merge: сохраняет полную историю, безопасен (не меняет существующие коммиты), понятен начинающим. Недостатки: история может быть запутанной (много merge commits).\n\nПреимущества rebase: чистая линейная история, проще понять последовательность изменений, легче использовать git bisect. Недостатки: переписывает историю (опасно для публичных веток), сложнее для новичков.\n\nРекомендация: merge для публичных веток (main), rebase для локальных feature-веток перед merge.",
        "example": "# Workflow с merge\ngit checkout main\ngit pull\ngit checkout feature/add-login\ngit merge main  # обновить feature-ветку\n# ... разработка ...\ngit checkout main\ngit merge feature/add-login  # влить в main\n\n# Workflow с rebase\ngit checkout main\ngit pull\ngit checkout feature/add-login\ngit rebase main  # обновить feature-ветку\n# ... разработка ...\ngit checkout main\ngit merge feature/add-login  # fast-forward merge (чистая история)",
        "key_points": [
          "Merge — безопасен, сохраняет историю, подходит для публичных веток",
          "Rebase — чистая история, но переписывает коммиты",
          "Golden Rule: никогда не rebase публичные ветки"
        ]
      },
      {
        "id": 9,
        "title": "git push, git pull, git fetch",
        "content": "git push отправляет локальные коммиты на удалённый репозиторий (remote). git pull загружает изменения с remote и автоматически делает merge. git fetch загружает изменения, но не мерджит — позволяет проверить перед интеграцией.\n\ngit pull = git fetch + git merge. Если хочешь контроль — используй git fetch, затем git merge или git rebase вручную.\n\nПервый push ветки: git push -u origin feature/add-login. -u (--set-upstream) устанавливает связь между локальной и удалённой веткой — дальше можно просто git push.",
        "example": "# Отправить коммиты на remote\ngit push\n\n# Первый push новой ветки\ngit push -u origin feature/add-login\n\n# Принудительный push (опасно! перезаписывает remote)\ngit push --force\ngit push --force-with-lease  # безопаснее (проверяет что не перезапишешь чужие коммиты)\n\n# Загрузить изменения с remote\ngit pull\n\n# Загрузить без автоматического merge\ngit fetch\ngit log origin/main  # посмотреть что изменилось\ngit merge origin/main  # влить вручную",
        "key_points": [
          "git push — отправить коммиты, git pull — загрузить и влить",
          "git fetch — загрузить без merge (для проверки)",
          "--force опасен — используй --force-with-lease"
        ]
      },
      {
        "id": 10,
        "title": "Best practices",
        "content": "Best practices Git: коммить часто (маленькие атомарные коммиты лучше больших), писать понятные commit messages, использовать ветки для фич/багфиксов, не коммитить сгенерированные файлы (используй .gitignore).\n\nGit workflow: популярные модели — Git Flow (feature/develop/release/hotfix ветки), GitHub Flow (main + feature branches), Trunk-Based Development (короткоживущие ветки, частые коммиты в main).\n\nБезопасность: не коммить секреты (пароли, токены, ключи), проверять .gitignore, использовать git-secrets или pre-commit hooks для автоматической проверки, регулярно делать git pull перед push.",
        "example": "# .gitignore (не коммитить эти файлы)\n# Зависимости\nnode_modules/\n__pycache__/\n*.pyc\n\n# Секреты\n.env\n*.key\n*.pem\n\n# IDE\n.vscode/\n.idea/\n\n# Временные файлы\n*.log\n*.tmp\n\n# Проверить что будет проигнорировано\ngit status --ignored\n\n# Добавить в .gitignore и удалить из репозитория\necho \"secrets.txt\" >> .gitignore\ngit rm --cached secrets.txt\ngit commit -m \"Remove secrets from repo\"",
        "key_points": [
          "Атомарные коммиты — один коммит = одно логическое изменение",
          ".gitignore — обязателен (зависимости, секреты, временные файлы)",
          "Не коммить секреты — используй переменные окружения"
        ]
      }
    ],
    "tasks": [
      31,
      32,
      33
    ]
  },
  "docker": [
    {
      "id": "docker_1",
      "title": "Docker vs контейнеры",
      "content": "**Что такое контейнер?**\n\n**Контейнер** — это изолированный процесс с собственной файловой системой, сетью и ресурсами.\n\n**Отличия от VM:**\n\n| VM | Контейнер |\n|-----|-----|\n| Полная ОС | Общее ядро хоста |\n| Гипервизор | Docker Engine |\n| ГБ дискового места | МБ |\n| Минуты на старт | Секунды на старт |\n| Тяжеловесный | Легковесный |\n\n**Ключевые технологии Linux:**\n- **namespaces** — изоляция (процессы, сеть, mount)\n- **cgroups** — лимиты ресурсов (CPU, память)\n- **overlayfs** — слоёная файловая система\n\n**Для чего Docker:**\n- Одинаковое окружение (dev = prod)\n- Быстрое развёртывание\n- Микросервисы",
      "example": "docker run -d --name nginx -p 80:80 nginx:latest"
    },
    {
      "id": "docker_2",
      "title": "Docker layers (слои образа)",
      "content": "**Как устроены Docker образы?**\n\nОбраз состоит из **readonly слоёв** (layers). Каждая инструкция в Dockerfile создаёт новый слой.\n\n**Пример Dockerfile:**\n```dockerfile\nFROM ubuntu:20.04        # Слой 1 (base)\nRUN apt-get update       # Слой 2\nRUN apt-get install nginx # Слой 3\nCOPY index.html /var/www/ # Слой 4\n```\n\n**Преимущества слоёв:**\n- **Кэширование** — неизменённые слои переиспользуются\n- **Экономия места** — одинаковые слои используются несколькими образами\n\n**Container layer:**\nПри запуске контейнера создаётся **writable layer** поверх readonly слоёв.\n\n**Команды для просмотра:**\n```bash\ndocker history <image>    # История слоёв\ndocker inspect <image>    # Детальная информация\n```\n\n**Best practice:** объединяй RUN команды чтобы уменьшить количество слоёв:\n```dockerfile\nRUN apt-get update && apt-get install -y nginx\n```",
      "example": "docker history nginx:latest"
    }
  ],
  "tcpip": [
    {
      "id": "tcpip_1",
      "title": "TCP Three-Way Handshake",
      "content": "**Как устанавливается TCP соединение?**\n\n```\nClient          Server\n  |               |\n  |---SYN-------->|  (1) Клиент: \"Хочу подключиться\"\n  |               |\n  |<--SYN-ACK-----|  (2) Сервер: \"OK, подтверждаю\"\n  |               |\n  |---ACK-------->|  (3) Клиент: \"Подтверждаю обратно\"\n  |               |\n  [ СОЕДИНЕНИЕ УСТАНОВЛЕНО ]\n```\n\n**Флаги:**\n- **SYN** (Synchronize) — запрос на соединение\n- **ACK** (Acknowledgment) — подтверждение\n\n**3 шага:** SYN → SYN-ACK → ACK\n\n**Зачем 3 шага?** Обе стороны должны подтвердить готовность.",
      "example": "sudo tcpdump -n 'tcp[tcpflags] & tcp-syn != 0' port 80"
    },
    {
      "id": "tcpip_2",
      "title": "TCP Termination (завершение)",
      "content": "**Как завершается TCP соединение?**\n\n**Graceful shutdown (4 шага):**\n```\nClient          Server\n  |---FIN-------->|  (1) Клиент: \"Я закончил\"\n  |<--ACK---------|  (2) Сервер: \"Понял\"\n  |<--FIN---------|  (3) Сервер: \"Я тоже закончил\"\n  |---ACK-------->|  (4) Клиент: \"Понял\"\n```\n\n**Экстренный обрыв:**\n```\n  |---RST-------->|  Экстренное завершение\n```\n\n**Когда бывает RST:**\n- Порт закрыт (connection refused)\n- Timeout истёк\n- Приложение крашнулось",
      "example": "netstat -an | grep CLOSE_WAIT"
    },
    {
      "id": "tcpip_3",
      "title": "TCP vs UDP",
      "content": "**Отличия протоколов:**\n\n| TCP | UDP |\n|-----|-----|\n| Connection-oriented | Connectionless |\n| Гарантия доставки | Без гарантии |\n| Порядок сохраняется | Порядок не гарантирован |\n| Медленнее (overhead) | Быстрее |\n| HTTP, SSH, FTP | DNS, VoIP, видео |\n\n**Когда TCP:**\n- Нужна гарантия доставки\n- Порядок пакетов важен\n- Пример: веб, SSH, база данных\n\n**Когда UDP:**\n- Скорость важнее надёжности\n- Потеря пакета не критична\n- Пример: DNS, стриминг, игры",
      "example": "# TCP\ntelnet google.com 80\n\n# UDP\ndig @8.8.8.8 google.com"
    },
    {
      "id": "tcpip_4",
      "title": "Connection refused vs timeout",
      "content": "**Ошибки подключения:**\n\n**Connection refused:**\n- Хост доступен, порт закрыт\n- Сервер отправил RST пакет\n- Пример: nginx не запущен\n\n**Connection timeout:**\n- Нет ответа от хоста\n- Firewall блокирует\n- Хост недоступен\n\n**Как различить:**\n```bash\n# Refused — мгновенно\ntelnet 192.168.1.1 9999\n# connect to address 192.168.1.1 port 9999: Connection refused\n\n# Timeout — ждёт ~30 сек\ntelnet 10.0.0.1 80\n# Trying 10.0.0.1... [долго висит]\n```",
      "example": "telnet google.com 80  # OK\ntelnet google.com 9999  # Refused"
    },
    {
      "id": "tcpip_5",
      "title": "Порты и протоколы",
      "content": "**Известные порты (well-known, 0-1023):**\n\n- **22** — SSH\n- **80** — HTTP\n- **443** — HTTPS\n- **3306** — MySQL\n- **5432** — PostgreSQL\n- **6379** — Redis\n- **27017** — MongoDB\n\n**Ephemeral ports (49152-65535):**\n- Используются клиентом для исходящих соединений\n- Выделяются автоматически ОС\n\n**Проверка портов:**\n```bash\n# Кто слушает порт 80?\nsudo ss -tlnp | grep :80\n\n# Какие порты открыты?\nsudo ss -tuln\n```",
      "example": "sudo lsof -i :80  # Кто использует порт 80"
    }
  ],
  "systemd": [
    {
      "id": "systemd_1",
      "title": "systemctl (управление сервисами)",
      "content": "**Основные команды systemctl:**\n\n**Статус:**\n```bash\nsystemctl status nginx\n```\n\n**Управление:**\n```bash\nsystemctl start nginx     # Запустить\nsystemctl stop nginx      # Остановить\nsystemctl restart nginx   # Перезапустить\nsystemctl reload nginx    # Перезагрузить конфиг (без downtime)\n```\n\n**Автозапуск:**\n```bash\nsystemctl enable nginx    # Включить автозапуск\nsystemctl disable nginx   # Выключить\nsystemctl is-enabled nginx  # Проверить статус\n```\n\n**Списки:**\n```bash\nsystemctl list-units --type=service  # Все сервисы\nsystemctl list-units --state=failed  # Failed сервисы\n```",
      "example": "systemctl status nginx"
    },
    {
      "id": "systemd_2",
      "title": "journalctl (логи systemd)",
      "content": "**Просмотр логов через journalctl:**\n\n**Базовые команды:**\n```bash\njournalctl -u nginx           # Логи nginx\njournalctl -u nginx -f        # Follow (как tail -f)\njournalctl -u nginx -n 50     # Последние 50 строк\n```\n\n**Фильтры по времени:**\n```bash\njournalctl --since \"2026-03-30 09:00\"\njournalctl --since today\njournalctl --since \"1 hour ago\"\n```\n\n**Фильтры по уровню:**\n```bash\njournalctl -p err             # Только ошибки\njournalctl -p warning         # Warning и выше\n```\n\n**Комбинации:**\n```bash\njournalctl -u nginx -p err --since today\n```",
      "example": "journalctl -u nginx -f"
    },
    {
      "id": "systemd_3",
      "title": "systemd unit files",
      "content": "**Где находятся unit файлы:**\n\n- `/etc/systemd/system/` — кастомные юниты\n- `/lib/systemd/system/` — системные юниты\n\n**Структура unit файла:**\n```ini\n[Unit]\nDescription=My Service\nAfter=network.target\n\n[Service]\nType=simple\nExecStart=/usr/bin/myapp\nRestart=on-failure\n\n[Install]\nWantedBy=multi-user.target\n```\n\n**Применение изменений:**\n```bash\nsudo systemctl daemon-reload  # Перечитать конфиги\nsudo systemctl restart myservice\n```\n\n**Просмотр unit:**\n```bash\nsystemctl cat nginx\n```",
      "example": "systemctl cat nginx"
    }
  ],
  "network_utils": [
    {
      "id": "network_utils_1",
      "title": "ss (современная замена netstat)",
      "content": "**ss — Socket Statistics (быстрее netstat)**\n\n**Основные команды:**\n```bash\nss -tln              # TCP listening порты\nss -tuln             # TCP + UDP listening\nss -t                # Все TCP соединения\nss -u                # Все UDP соединения\n```\n\n**С процессами (нужен sudo):**\n```bash\nsudo ss -tlnp        # TCP listening с процессами\nsudo ss -tlnp | grep :80  # Кто слушает порт 80\n```\n\n**Состояния соединений:**\n```bash\nss -tan              # TCP со всеми состояниями\nss -tan state established  # Только ESTABLISHED\n```\n\n**Полезные ключи:**\n- `-t` — TCP\n- `-u` — UDP\n- `-l` — listening\n- `-n` — числовой вид (без DNS)\n- `-p` — показать процессы",
      "example": "sudo ss -tlnp | grep :80  # Кто слушает порт 80"
    },
    {
      "id": "network_utils_2",
      "title": "netstat (классическая утилита)",
      "content": "**netstat — старая, но всё ещё используемая утилита**\n\n**Основные команды:**\n```bash\nnetstat -tln         # TCP listening порты\nnetstat -tuln        # TCP + UDP listening\nnetstat -r           # Routing table\nnetstat -i           # Сетевые интерфейсы\n```\n\n**С процессами:**\n```bash\nsudo netstat -tlnp   # TCP listening с процессами\n```\n\n**Статистика:**\n```bash\nnetstat -s           # Статистика по протоколам\n```\n\n**⚠️ Замечание:** На современных системах рекомендуется использовать `ss` вместо `netstat` (быстрее и активно развивается).",
      "example": "netstat -r  # Routing table"
    },
    {
      "id": "network_utils_3",
      "title": "ip (современная замена ifconfig)",
      "content": "**ip — Swiss Army knife для сетевых настроек**\n\n**IP адреса:**\n```bash\nip addr show         # Все IP адреса\nip a                 # Короткая форма\nip a show dev eth0   # Только eth0\n```\n\n**Routing table:**\n```bash\nip route show        # Таблица маршрутизации\nip r                 # Короткая форма\nip route get 8.8.8.8 # Маршрут до конкретного хоста\n```\n\n**Интерфейсы:**\n```bash\nip link show         # Сетевые интерфейсы\nip link set eth0 up  # Включить интерфейс\n```\n\n**Добавить/удалить IP:**\n```bash\nsudo ip addr add 192.168.1.100/24 dev eth0\nsudo ip addr del 192.168.1.100/24 dev eth0\n```",
      "example": "ip route get 8.8.8.8  # Маршрут до 8.8.8.8"
    },
    {
      "id": "network_utils_4",
      "title": "tcpdump (захват пакетов)",
      "content": "**tcpdump — анализ сетевого трафика**\n\n**Базовые команды:**\n```bash\nsudo tcpdump              # Весь трафик\nsudo tcpdump -i eth0      # Только eth0\nsudo tcpdump -i any       # Все интерфейсы\n```\n\n**Фильтры по порту:**\n```bash\nsudo tcpdump port 80      # HTTP трафик\nsudo tcpdump port 443     # HTTPS трафик\n```\n\n**Фильтры по хосту:**\n```bash\nsudo tcpdump host 8.8.8.8      # От/до 8.8.8.8\nsudo tcpdump src 192.168.1.1   # Только от хоста\nsudo tcpdump dst 192.168.1.1   # Только до хоста\n```\n\n**Сохранение в файл:**\n```bash\nsudo tcpdump -w capture.pcap port 80\nsudo tcpdump -r capture.pcap  # Чтение из файла\n```\n\n**TCP флаги:**\n```bash\nsudo tcpdump 'tcp[tcpflags] & tcp-syn != 0' port 80  # SYN пакеты\n```",
      "example": "sudo tcpdump -i any port 80 -n  # HTTP трафик"
    },
    {
      "id": "network_utils_5",
      "title": "lsof для сетевых соединений",
      "content": "**lsof — не только файлы, но и сокеты!**\n\n**Все сетевые соединения:**\n```bash\nlsof -i              # Все сокеты\nlsof -i TCP          # Только TCP\nlsof -i UDP          # Только UDP\n```\n\n**По порту:**\n```bash\nlsof -i :80          # Кто слушает порт 80\nlsof -i :1-1024      # Все well-known порты\n```\n\n**По хосту:**\n```bash\nlsof -i @8.8.8.8     # Соединения с 8.8.8.8\n```\n\n**По процессу:**\n```bash\nlsof -p 1234 -i      # Только сетевые соединения процесса\n```\n\n**По пользователю:**\n```bash\nlsof -u nginx -i     # Все соединения пользователя nginx\n```\n\n**Комбинации (AND):**\n```bash\nlsof -i -a -p 1234   # Сетевые соединения процесса 1234\n```",
      "example": "sudo lsof -i :443  # Кто слушает HTTPS"
    }
  ],
  "linux_files": [
    {
      "id": "linux_files_1",
      "title": "/etc/nsswitch.conf",
      "content": "**Name Service Switch — очерёдность поиска имён**\n\n**Назначение:**\n- Определяет где искать имена хостов, пользователей, группы\n- Контролирует порядок: файлы → DNS → LDAP и т.д.\n\n**Пример (часть файла):**\n```\nhosts:      files dns\npasswd:     files systemd\ngroup:      files systemd\nshadow:     files\n```\n\n**Что означает `hosts: files dns`:**\n1. Сначала смотрим `/etc/hosts` (files)\n2. Потом идём в DNS\n\n**Как проверить:**\n```bash\ngetent hosts google.com   # Использует nsswitch.conf\n```\n\n**Где лежит:**\n```bash\n/etc/nsswitch.conf\n```",
      "example": "getent hosts google.com  # Использует nsswitch"
    },
    {
      "id": "linux_files_2",
      "title": "/etc/resolv.conf",
      "content": "**DNS resolver configuration**\n\n**Назначение:**\n- Список DNS серверов\n- Search domain для коротких имён\n\n**Пример файла:**\n```\nnameserver 8.8.8.8\nnameserver 1.1.1.1\nsearch example.com\n```\n\n**Директивы:**\n- `nameserver` — DNS сервер (до 3 шт.)\n- `search` — поиск в домене (ping server → server.example.com)\n- `domain` — локальный домен\n\n**⚠️ Важно:** На современных системах файл управляется:\n- **NetworkManager** → создаёт автоматически\n- **systemd-resolved** → симлинк на `/run/systemd/resolve/stub-resolv.conf`\n\n**Где лежит:**\n```bash\n/etc/resolv.conf\n```",
      "example": "cat /etc/resolv.conf"
    },
    {
      "id": "linux_files_3",
      "title": "/etc/hosts",
      "content": "**Статический DNS (локальные записи)**\n\n**Назначение:**\n- Переопределить DNS для конкретных хостов\n- Локальные алиасы\n- Блокировка доменов (→ 127.0.0.1)\n\n**Пример файла:**\n```\n127.0.0.1   localhost\n::1         localhost\n192.168.1.100  myserver\n192.168.1.200  db.local\n```\n\n**Как добавить запись:**\n```bash\necho \"192.168.1.100 myserver\" | sudo tee -a /etc/hosts\n```\n\n**Как проверить:**\n```bash\nping myserver  # Использует /etc/hosts\ngetent hosts myserver\n```\n\n**Приоритет:** Если в `nsswitch.conf` прописано `hosts: files dns`, то `/etc/hosts` проверяется РАНЬШЕ DNS!\n\n**Где лежит:**\n```bash\n/etc/hosts\n```",
      "example": "getent hosts localhost"
    },
    {
      "id": "linux_files_4",
      "title": "/proc/ (виртуальная ФС)",
      "content": "**procfs — информация о процессах и системе**\n\n**⚠️ Важно:** Это не реальные файлы! Генерируется ядром в реальном времени.\n\n**Информация о системе:**\n```bash\n/proc/cpuinfo        # Информация о CPU\n/proc/meminfo        # Информация о памяти\n/proc/uptime         # Время работы системы\n/proc/version        # Версия ядра\n/proc/loadavg        # Load average\n```\n\n**Информация о процессах:**\n```bash\n/proc/<PID>/cmdline  # Командная строка процесса\n/proc/<PID>/cwd      # Рабочая директория (symlink)\n/proc/<PID>/environ  # Переменные окружения\n/proc/<PID>/fd/      # Открытые файлы\n/proc/<PID>/status   # Статус процесса\n```\n\n**Примеры:**\n```bash\ncat /proc/cpuinfo\ncat /proc/meminfo\nls -l /proc/self/fd  # Открытые файлы текущего процесса\n```",
      "example": "cat /proc/cpuinfo | head -20"
    },
    {
      "id": "linux_files_5",
      "title": "/sys/ (sysfs)",
      "content": "**sysfs — информация об устройствах и ядре**\n\n**Назначение:**\n- Конфигурация устройств\n- Информация о драйверах\n- Управление параметрами ядра\n\n**Основные директории:**\n```bash\n/sys/class/          # Устройства по классам (net, block, etc)\n/sys/devices/        # Дерево устройств\n/sys/block/          # Блочные устройства\n/sys/module/         # Загруженные модули ядра\n```\n\n**Примеры:**\n```bash\n# Информация о сетевых интерфейсах\nls /sys/class/net/\ncat /sys/class/net/eth0/address  # MAC адрес\n\n# Информация о блочных устройствах\nls /sys/block/\ncat /sys/block/sda/size  # Размер диска (в блоках)\n```\n\n**⚠️ Важно:** Изменения в `/sys/` действуют мгновенно, но НЕ сохраняются после перезагрузки!",
      "example": "ls /sys/class/net/"
    }
  ],
  "http_methods": [
    {
      "id": "http_methods_1",
      "title": "HTTP методы (все 7 основных)",
      "content": "**Основные HTTP методы:**\n\n**1. GET** — получить ресурс\n- Параметры в URL (query string)\n- Идемпотентный (можно повторять)\n- Кэшируется\n\n**2. POST** — создать ресурс\n- Параметры в теле запроса\n- НЕ идемпотентный\n\n**3. PUT** — обновить весь ресурс\n- Заменяет ресурс целиком\n- Идемпотентный\n\n**4. PATCH** — частичное обновление\n- Обновляет только указанные поля\n- НЕ идемпотентный\n\n**5. DELETE** — удалить ресурс\n- Идемпотентный\n\n**6. HEAD** — как GET, но без тела ответа\n- Только заголовки\n\n**7. OPTIONS** — доступные методы\n- Используется для CORS preflight",
      "example": "curl -X DELETE https://api.example.com/users/123"
    },
    {
      "id": "http_methods_2",
      "title": "GET vs POST",
      "content": "**Чем отличаются GET и POST?**\n\n**GET:**\n- Параметры в URL (`?key=value&foo=bar`)\n- Идемпотентный (можно повторять)\n- Кэшируется браузером\n- Лимит длины URL (~2000 символов)\n- Виден в истории браузера\n- Используется для ПОЛУЧЕНИЯ данных\n\n**POST:**\n- Параметры в теле запроса\n- НЕ идемпотентный (создаёт новый ресурс)\n- Не кэшируется\n- Нет лимита размера (зависит от сервера)\n- Не виден в истории\n- Используется для СОЗДАНИЯ данных\n\n**Примеры:**\n```bash\n# GET\ncurl \"https://api.example.com/users?name=John\"\n\n# POST\ncurl -X POST -d '{\"name\":\"John\"}' https://api.example.com/users\n```",
      "example": "curl -X POST -d '{\"name\":\"test\"}' https://httpbin.org/post"
    },
    {
      "id": "http_methods_3",
      "title": "PUT vs PATCH",
      "content": "**В чём разница между PUT и PATCH?**\n\n**PUT — полная замена ресурса:**\n```json\n# Было:\n{\"name\": \"John\", \"age\": 30, \"city\": \"Moscow\"}\n\n# PUT запрос:\n{\"name\": \"John Smith\"}\n\n# Стало:\n{\"name\": \"John Smith\"}  // age и city УДАЛИЛИСЬ!\n```\n\n**PATCH — частичное обновление:**\n```json\n# Было:\n{\"name\": \"John\", \"age\": 30, \"city\": \"Moscow\"}\n\n# PATCH запрос:\n{\"name\": \"John Smith\"}\n\n# Стало:\n{\"name\": \"John Smith\", \"age\": 30, \"city\": \"Moscow\"}  // Остальное осталось!\n```\n\n**Идемпотентность:**\n- PUT — идемпотентный (повторный вызов даёт тот же результат)\n- PATCH — НЕ идемпотентный\n\n**Когда что использовать:**\n- PUT — когда обновляешь ВСЕ поля\n- PATCH — когда обновляешь ЧАСТЬ полей",
      "example": "curl -X PATCH -d '{\"age\":31}' https://api.example.com/users/123"
    },
    {
      "id": "http_methods_4",
      "title": "Идемпотентность HTTP методов",
      "content": "**Что такое идемпотентность?**\n\nМетод идемпотентный, если **повторные вызовы дают тот же результат**, что и первый.\n\n**Идемпотентные методы:**\n- **GET** — получить данные (можно повторять)\n- **PUT** — заменить ресурс (повторная замена даёт тот же результат)\n- **DELETE** — удалить ресурс (повторное удаление несуществующего → тот же результат)\n- **HEAD** — получить заголовки (можно повторять)\n- **OPTIONS** — получить методы (можно повторять)\n\n**НЕ идемпотентные методы:**\n- **POST** — создать ресурс (каждый вызов создаёт НОВЫЙ ресурс)\n- **PATCH** — обновить часть (зависит от реализации)\n\n**Примеры:**\n```bash\n# Идемпотентный (можно повторять)\nDELETE /users/123  → 200 OK\nDELETE /users/123  → 404 Not Found (но результат тот же — пользователя нет)\n\n# НЕ идемпотентный\nPOST /users {\"name\":\"John\"}  → создан user с id=1\nPOST /users {\"name\":\"John\"}  → создан user с id=2 (ДРУГОЙ результат!)\n```",
      "example": "# Идемпотентный DELETE\ncurl -X DELETE https://api.example.com/users/123"
    }
  ],
  "troubleshooting": [
    {
      "id": "troubleshooting_1",
      "title": "Диагностика: сервис не отвечает",
      "content": "**Сценарий:** Nginx не отвечает на запросы\n\n**Шаги диагностики:**\n\n**1. Проверить статус сервиса:**\n```bash\nsystemctl status nginx\n```\nВозможные проблемы:\n- `inactive (dead)` — не запущен\n- `failed` — ошибка запуска\n\n**2. Проверить логи:**\n```bash\njournalctl -u nginx -n 50\n/var/log/nginx/error.log\n```\n\n**3. Проверить порт:**\n```bash\nsudo ss -tlnp | grep :80\n```\nВозможные проблемы:\n- Порт не слушается — nginx не запустился\n- Порт занят другим процессом\n\n**4. Проверить конфиг:**\n```bash\nnginx -t  # Тест синтаксиса конфига\n```\n\n**5. Проверить firewall:**\n```bash\nsudo iptables -L -n | grep 80\n```",
      "example": "systemctl status nginx && sudo ss -tlnp | grep :80"
    },
    {
      "id": "troubleshooting_2",
      "title": "Диагностика: high load average",
      "content": "**Сценарий:** Load average высокий (>4 на 4-ядерном CPU)\n\n**Шаги диагностики:**\n\n**1. Проверить load:**\n```bash\nuptime\n# load average: 8.23, 6.45, 4.12  (1min, 5min, 15min)\n```\n\n**2. Найти процессы-потребители:**\n```bash\ntop\nhtop  # Лучше визуализация\n```\nСмотрим на:\n- `%CPU` — процессор\n- `%MEM` — память\n- `load average` — очередь процессов\n\n**3. Проверить I/O wait:**\n```bash\niostat -x 1  # Обновление каждую секунду\n```\n`%iowait` > 20% — проблема с диском!\n\n**4. Проверить диск:**\n```bash\ndf -h          # Свободное место\ndu -sh /*      # Кто жрёт место\niotop          # Кто грузит диск\n```\n\n**5. Проверить процессы в состоянии D (uninterruptible sleep):**\n```bash\nps aux | awk '$8==\"D\" {print $0}'\n```\nD-state = процесс ждёт I/O (диск, сеть)",
      "example": "uptime && top -bn1 | head -20"
    },
    {
      "id": "troubleshooting_3",
      "title": "Диагностика: out of memory",
      "content": "**Сценарий:** Приложение крашится с OOM (Out Of Memory)\n\n**Шаги диагностики:**\n\n**1. Проверить память:**\n```bash\nfree -h\n```\nСмотрим на:\n- `available` — доступная память\n- `swap` — используется ли swap\n\n**2. Найти memory hogs:**\n```bash\nps aux --sort=-%mem | head -10  # Топ-10 по памяти\n```\n\n**3. Проверить OOM killer logs:**\n```bash\ndmesg | grep -i \"out of memory\"\ndmesg | grep -i \"killed process\"\n```\n\n**4. Проверить лимиты процесса:**\n```bash\ncat /proc/<PID>/limits\n```\n\n**5. Для Docker контейнеров:**\n```bash\ndocker stats                    # Реалтайм статистика\ndocker inspect <container>      # Проверить memory limit\n```\n\n**Решения:**\n- Увеличить память\n- Оптимизировать приложение\n- Добавить swap (временное решение)\n- Для Docker: увеличить `--memory` лимит",
      "example": "free -h && ps aux --sort=-%mem | head -10"
    }
  ]
}